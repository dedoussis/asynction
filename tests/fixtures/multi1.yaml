asyncapi: 2.2.0
info:
  title: Multi1
  version: 1.0.0
servers:
  test:
    url: https://locallhost/test
    protocol: wss
  test2:
    url: https://locallhost/test
    protocol: wss
channels:
  /channel1:
    subscripe:
      message:
        schema:
          type: string
    x-handlers:
      connect: tests.fixtures.handlers.connect_true

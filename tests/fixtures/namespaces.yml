channels:
  user/signedup:
    publish:
      operationId: tests.fixtures.handlers.my_handler
      message:
        $ref: '#/components/messages/UserSignedUp'
    subscribe:
      message:
        $ref: '#/components/messages/UserSignedUp'
x-namespaces:
  /:
    description: Main namespace
    errorHandler: test.fixtures_handlers.my_other_handler
  /user:
    errorHandler: test.fixtures_handlers.my_other_handler
components:
  messages:
    UserSignedUp:
      payload:
        type: object

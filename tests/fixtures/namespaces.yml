channels:
  user/signedup:
    publish:
      operationId: tests.fixtures.handlers.user_signedup
      message:
        $ref: '#/components/messages/UserSignedUp'
    subscribe:
      message:
        $ref: '#/components/messages/UserSignedUp'
x-namespaces:
  /:
    description: Main namespace
    errorHandler: test.fixtures_handlers.default_error_handler
  /user:
    errorHandler: test.fixtures_handlers.user_signedup_error
components:
  messages:
    UserSignedUp:
      payload:
        type: object

channels:
  signedup:
    subscribe:
      operationId: tests.fixtures.handlers.my_handler
      message:
        $ref: '#/components/messages/UserSignedUp'
components:
  messages:
    UserSignedUp:
      payload:
        type: object
